[project]
name = "agentrouter-check-in"
version = "1.0.0"
description = "AgentRouter local daily check-in via persistent GitHub OAuth browser profiles"
requires-python = ">=3.11"
dependencies = [
  "cloakbrowser==0.3.31",
  "python-dotenv>=1.0.0",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 120
